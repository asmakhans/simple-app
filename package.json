{
  "name": "simple-app",
  "version": "1.0.0",
  "description": "an app created simply for being simple",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Asma Khan",
  "license": "ISC",
  "dependencies": {
    "@slack/bolt": "^3.8.1",
    "dotenv": "^10.0.0"
  }
}
